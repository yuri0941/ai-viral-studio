# AGENTS.md — прошивка для ИИ-кодера (ai-viral-studio)

Контекст проекта: `PROJECT_CONTEXT.md`. История работ: `PROGRESS_REPORT.md` (не дублировать, только дополнять записями по шаблону). Стандарт UI: `docs/UI_QUALITY_GATE.md`.

## Правила работы (v9)

- Работаешь сам, без сварма субагентов.
- 1 ответ = правка 1–3 файлов + `node --check` изменённого. Размышления ≤3 строк, без анонсов.
- Todo текстом (✅/▶/⬜). Wip-коммиты каждые 20–30 мин. `git add` строго по списку файлов. `reports/` не коммитить.
- Ветка от свежего `origin/main`. Один PR = одна зона (payments / storefront / owner / …). Финал задачи: `git push -u origin <ветка>` → отчёт с живой compare-ссылкой, `diff --stat`, скринами/выводом проверок и записью в `PROGRESS_REPORT.md`. **Отчёт без пуша = не сдан.**
- Никакого нового функционала сверх списка задач батча.
- Любое действие вне локального кода (merge, force-redeploy, конфиги/ключи/цены, тест-платежи) — только после approve владельца.
- Негативные тесты в каждом батче, затрагивающем доступ: admin/client дёргает owner-API → 403. Гард-матрица (`qaSecurityFlow`) должна оставаться зелёной.
- Перед сдачей: `npm run build` (frontend) 0 ошибок; `node scripts/qa-launch.mjs` — единая сводка всех QA зелёная; `node scripts/i18n-parity.mjs` чист.
- [QA-FIX] Батч НЕ сдан, пока CI на GitHub (Actions → qa) красный, даже если локально всё зелёное. Локальный прогон ≠ CI (свежая БД, медленный раннер, троттлинг rAF). Расхождение локаль/CI — обязательная строка в отчёте. Флаки «зелёно локально / красно в CI» лечить корнем (доказательство репродукцией с CI-условиями), не перезапуском.
- [QA-FIX] driver.js 1.8.0: `destroy()` в окне entry-анимации (400ms rAF) молча НЕ вызывает `onDestroyed` — важные колбэки (флаги, синк) вешать ДО `destroy()` или на `onDestroyStarted`, не только на `onDestroyed`.

## Экономия токенов

- Поиск по коду — точечно (grep/glob), файлы целиком не перечитывать без нужды.
- Доки библиотек — context7. Скрины — playwright (готовые шаблоны в `scripts/*-shots.mjs`).
- Секреты ЮKassa — только кабинет владельца/MongoDB, НЕ в репо/логах/отчётах (маскируем: `live_...O730`).

## Вопросы владельцу (правило молчания)

- Локальные диалоги вопросов (AskUserQuestion) НЕ используются. Вопрос владельцу — только через `node scripts/ask-owner.mjs` (дублируется в TG owner-бота и в терминал, первый ответ побеждает).
- Вопросы владельцу в процессе батча ЗАПРЕЩЕНЫ, кроме реального блокера (нет доступа/данных, противоречие ТЗ). Всё остальное — решать самому безопасным вариантом.
- Нет ответа 15 минут (считается от отправки в оба канала) → таймаут: самый безопасный вариант, работа продолжается, в отчёт строка «решил сам: X, причина: Y».
- ИСКЛЮЧЕНИЕ: на вопросах ВЫБОРА владельца (дизайн/варианты, цены, публикации, что применять в прод) таймаут-дефолта НЕТ — вопрос ждёт ответа без срока, блокируемый кусок пропускается, работа идёт по остальному. Таймаут-дефолт допустим только на технических мелочах (формулировки, порядок правок, таймауты).
- APPROVE-ЗОНА (деньги, тарифы/PlanConfig, безопасность, удаление данных, секреты) — сам НЕ решать никогда: пропустить кусок, в отчёт «ждёт решения владельца: <что именно>».
- Пуш батча — сам; мерж в main — только ✅ владельца в TG.
- Локальный backend НЕ трогает webhook ботов: `setWebhook`/`deleteWebhook` только на прод-хосте (гард `backend/utils/tgWebhookGuard.js`), локально — пропуск без polling.

## НЕ ЛОМАТЬ (красные зоны)

- Платежи/ЮKassa (создание платежа, webhook-верификация, refund).
- `PlanConfig` и тарифы, редактор цен, витрина пакетов кредитов, реферальная механика (`OwnerSettings.referralPercent`).
- Боты (Telegram), лендинг, рубильники (owner feature flags), rate-limit, view-as.
- Гард-матрицу Б5 (authorize/role checks ослаблять запрещено).
- Логику entitlement/снапшотов аддонов (`UserAddon.includesSnapshot/featuresSnapshot`).
- Webhook-гард (`backend/utils/tgWebhookGuard.js`), approve-зону, Hard Limit, Auto Downgrade.
- ask-owner / ownerFreeText / изоляцию owner-контура / OMEGA Control / двойной канал вопросов.
- Паузу CHANNEL-AUTO, голос владельца, удаление постов.
- Дизайн PR #60 (Фокус-чат / Люкс-хаб / Командный центр).

## Инфраструктура (факты)

- Прод: `https://aiviral-studio.ru` (Cloudflare Pages, SPA-fallback — ТОЛЬКО rewrite `/* /index.html 200` в `frontend/public/_redirects`, БЕЗ 301/302/308 и БЕЗ «!»). Запрещено: rewrite на `*.html`-цель (Pages каноникализирует `.html` 308-редиректом → петля, инцидент 06.09.2026: прод лежал из-за `/* /200.html 200`). Файл `200.html`/spa-200.mjs не нужны и запрещены.
- Любое изменение `_redirects`/redirect-логики — только с curl-пруфами на preview-деплое Pages ДО мержа (`/` + 2 прямых роута → 200, 0 редиректов). «Не сломано — не трогай» на проде важнее страховок.
- Backend: `https://aiviral-backend.onrender.com` (Render). БД локального backend ОТДЕЛЬНАЯ от прода (поэтому qa-скрипты проксируют prod-API на локальный); прод-БД — только через prod API / Render env.
- БД: MongoDB Atlas, у прода и локали РАЗНЫЕ базы. ShopID ЮKassa — live (`live_...O730`), ключи в Render env (прод) / кабинете владельца, hot-reload через `getProviderKey`.
- TG: owner-бот `@omega_aiviral_bot`, клиентский `@aiviral_alerts_bot`, канал `@aiviralstudio` (функциональные боты — оба; `qa-bots.mjs` проверяет webhook обоих).
- Тест-карта ЮKassa: `5555 5555 5555 4477 · 12/25 · CVV 000`.
- [PERF-AUDIT] Stripe-контур сохранён для будущего (рубильник владельца): `backend/routes/stripe.js` + монтирование в `server.js` + `services/stripeService.js` НЕ удалять. Контур НИКОГДА не тестировался end-to-end; до включения нужен отдельный батч STRIPE-ENABLE (тестовый платёж, валюты, возврат ~2–3 ч). Статус в owner-боте (`sendStatus`) честный: «не подключён» / «ключ есть, не протестирован» — НЕ ✅ по наличию env. Фронт: legacy CheckoutPage удалён в CHECKOUT-UNIFY, `/stripe-checkout` — orphan-роут без ссылок (lazy-чанк, в initial не входит).
- [PERF-AUDIT] AI-ключи в статусах (/keystatus, buildKeyDigest) = сохранённые валидные ключи из БД (`getProviderStatuses`); слоты PROVIDER_CHAIN ≠ ключи — в цепочке есть бесплатные/безключевые провайдеры, поэтому «ключей 4» при «11 провайдерах» — норма, а не баг. «Клиентов» в sendStats — фильтр `status != 'deleted' + isTestAccount != true` (как ownerController), не сырой count users.
- [CHAT-PRO] Выбор владельца (дизайн/макеты) = минимум 3 варианта скринами в TG одним альбомом; выбор из одного варианта не считается. Скрины каждого варианта: desktop тёмная+светлая + iPhone 390.
- [CHAT-PRO] Режим chat CreativeHub = «Люкс-хаб» (вариант B, выбор владельца 07.09.2026): `frontend/src/components/chat-pro/LuxeHubChat.jsx` — оболочка над `OmegaChat` (embedded, вся логика чата там: голос/вложения/превью/саппорт/апселл). Поиск по ленте — проп `searchQuery` в OmegaChat. Черновик инпута — localStorage `omega_chat_draft`. Старый UI не удалять до ✅ владельца.
- [CHAT-PRO-REWORK] Экран chat 1:1 по утверждённому эталону: НА экране только люкс-шапка (✦, поиск, пилюля баланса → QuotaDetailsModal, кнопка-меню), чистая лента, справа ≥821px одна колонка (BalanceRingCard + «В этом месяце», реальные `/users/me/quota` + `/scheduled-posts`). Панелей «Сессии»/«Режимы»/«Инсайты» НА ЭКРАНЕ НЕТ — инвентарь сохранён в шторке по кнопке-меню (все ширины, Esc/фон закрывает, `chatDrawerOpen` в CreativeHub; туда же бейдж роли и AutoPilot). Режимы дублируются переключателем в люкс-шапке (md+, role=tablist). Светлая тема = та же компоновка в светлых токенах: `.chat-pro-scope` CSS-варианты + `html.light .chat-pro-page/.chat-pro-embed` в `frontend/src/styles/luxury.css` (фон #f5f5f7, лента белая). Дизайн-гейт — `scripts/chat-pro-luxe-shots.mjs` (не входит в qa-launch — запускать отдельно при поднятых :18080/:4173).
- [CHAT-PRO-FIX] ВСЕ режимы хаба (chat/analyzer/viral) — единая люкс-компоновка: `isLuxeLayout = true` в CreativeHub (старые grid-экраны режимов недостижимы, НЕ удалены до ✅ владельца); меняются шапка (режим в подзаголовке), плейсхолдер (`inputPlaceholder` в OmegaChat), подсказки. Шторка «Меню чата» — СЛЕВА, solid (`.chat-pro-drawer` в luxury.css + light-оверрайды, без блюра), ≤360px, свайп с левого края, анимация 280ms (reduced-motion — нет). Владелец = безлимит: `/users/me/quota` отдаёт `unlimited:true` (Infinity в JSON превращается в null — владелец видел «0✦ из 10»), фронт (пилюля/кольцо/QuotaDetailsModal) показывает ∞ без плашек лимита и CTA пополнения. Скролл чата: скроллится ТОЛЬКО лента `.omega-chat-scroll` (страница 100dvh, не скроллится), авто-скролл вниз только у низа или после своего сообщения, кнопки ↓ (бейдж новых) / ↑ (>3 экранов), окно истории 30 + догрузка вверх со scroll-anchor (позиция не прыгает). Управление историей: ⋯ сессии (long-press на мобайле) → переименовать/удалить с подтверждением; ⋯ шапки → «Очистить историю» с подтверждением. QA: owner.test@aiviral-studio.ru в сидере createTestAccounts.
- [OMEGA-VIDEO ДОП-З1] Drag&drop + скрепка видео (mp4/mov/webm) в чате — расширение паттерна картинок в `OmegaChat.jsx`: чип Film (имя/вес/✕), XHR на `/api/upload/media` с прогрессом % и отменой (abort), обрыв сети → честная ошибка + «Повторить». Лимит веса — `OwnerSettings.mediaUploadLimitMb` (1–250, дефолт 250, кэш ≤60с): owner GET/POST `/api/owner/media-limit` (объявлены ВЫШЕ generic `router.post('/:entity')` в routes/owner.js — иначе Unknown entity), клиент читает `/api/upload/limits`, backend 413 `{limitMb, fileMb}` → тост «Лимит N МБ, файл M МБ». Кабинет: карточка в Подписках (SubscriptionsTab). После загрузки файл СРАЗУ в разбор: кадры на клиенте (video+canvas, ≤6, 640px jpeg) → `POST /api/omega/analyze-video-upload` (только свой `/uploads/<userId>/`, иначе 403; списание 1✦ через consumeGeneration; vision по кадрам через visionCore + синтез таймкоды/хук/удержание; smart-fallback = мок → 200 success:false ai_unavailable + refundGeneration, файл НЕ удаляется; после успеха файл авто-удаляется — нормализация пути с `replace(/\\/g,'/')`, Windows!). Цена «Анализ видео = 1✦ · осталось N✦» на чипе ДО анализа (фактическая стоимость сообщения; отдельного реестра цен действий в репо нет — ждёт REAL-DATA З5.3). CreativeHub = внешний режим чата: сообщения разбора идут через `injectMessages` из useOmegaChat (internalMessages лента не видит). i18n-интерполяция в кастомном t() — ТОЛЬКО `{{var}}` (не `{var}`). Гейт: `scripts/omega-video-upload-shots.mjs` (27 проверок, не входит в qa-launch — запускать отдельно при поднятых :18080/:4173).
- [OMEGA-VIDEO ДОП-2] Цены AI-действий и TTL — паттерном mediaUploadLimitMb, плоские поля OwnerSettings (реестр цен соберёт REAL-DATA, не создавать): `videoAnalysisCostCredits` (1✦ дефолт), `coverGenerationCostCredits` (1✦), `scriptGenerationCostCredits` (2✦), `videoStorageTtlHours` (0 = удалить файл сразу после разбора, дефолт; результат анализа в чате остаётся навсегда). Кэш ≤60с (getVideoSettings/invalidateVideoSettingsCache), owner GET/POST `/api/owner/video-settings` (ВЫШЕ generic /:entity), клиент читает цены/TTL из `/api/upload/limits`. Списание — `consumeGeneration(userId, role, {cost})`, возврат — `refundGeneration(userId, cost)` (фактическая цена, не хардкод). Учёт файлов — `MediaFile` (регистрация в routes/upload.js trackMediaFile best-effort); TTL после разбора — `services/videoStorage.js#applyVideoStorageTtl`; крон `cron/mediaCleanup.js`: deleteAt истёк → unlink, сироты (analyzedAt null >24ч) → unlink, сводка `sendOwnerAlert` в TG. Счётчик хранилища — `GET /api/upload/storage-usage` фактом с диска (`uploads/<userId>/`). Сценарий из мысли: `POST /api/omega/script-from-idea` (неполная цель → needClarification БЕЗ списания; цифры ниши — реальные из youtubeDataService, нет ключа → честно, выдуманные = провал honesty-гейта; драфт в Планировщик — POST /api/scheduler/posts status:'draft'). Обложки: `services/coverGenerator.js` — размер фактом под платформу (youtube/vk/telegram 1280×720, tiktok/instagram/shorts 1080×1920), текст 2–4 слова sharp-оверлеем с тёмной плашкой (AI текст НЕ рисует), `textHeightRatio ≥ COVER_TEXT_MIN_RATIO` — гейт читаемости в сетке 320px (≥10px рендера); `POST /api/omega/cover-variants` (3 варианта, сбой → refund). Конкуренты ниши: `GET /api/omega/niche-competitors` (YouTube реально; прочие → available:false+requiredKey+consoleLink, клиенту «скоро», владельцу «Требуется ключ» в ApiKeysTab через `apiKeys.requiredFor.<id>`). Гейт: `backend/scripts/qaOmegaVideo2.js` (входит в qa-launch) + расширенный `scripts/omega-video-upload-shots.mjs` (живая цена 3✦ на чипе).
- [ASK-OWNER-FIX] TG-канал ask-owner шлёт parse_mode=HTML: весь пользовательский текст (context/question/note) экранируется (`& < >`), иначе TG API 400 can't parse entities и сообщение молча не доходит (инцидент 07.09.2026 — отчёт с «<1МБ» не дошёл). Лимит 4096 — контекст обрезается, не роняет отправку. Код ответа TG API всегда в логе отправки.
- [HOTFIX-FINAL-2] Ключи провайдеров: выключенный в кабинете ключ = ПОЛНЫЙ запрет, env-фолбэки `|| process.env.*_API_KEY` после `getProviderKey` запрещены (мёртвый ключ поднимался обратно в ротацию). 401/403 от провайдера → `backend/utils/providerKeyGuard.js#disableProviderKey` (isActive=false в БД + стоп кэша + TG-алерт). Новый ключ — только владелец через кабинет.
- [HOTFIX-FINAL-2] Waitlist лендинга (`/api/public/waitlist`) пишет в Mongo (`Waitlist`), TG-алерт владельцу на каждую новую заявку. In-memory хранилища для заявок запрещены (Render рестартит — данные умирали).
- [HOTFIX-FINAL-2] Owner-бот: `/balance email`, `/givecredits email N` (owner-only, через `creditGenerations`). Каждый onText-хендлер обязан иметь try/catch с ответом — молчаливые падения = баг.
- [HOTFIX-FINAL-2] House-ads: `frontend/src/components/ads/HouseAdSlot.jsx` (спека Р3): чат=топ-баннер ≤34px (все ширины, инпут не сдвигает), сайдбар-карточка ≥1440, пилюля 768–1439, нижняя плашка <768 (не на /creative-hub — там FAB/навбар). Ротация аддоны→апгрейд→рефералка→YT-разведка 10с, crossfade 275ms, pulse 3.5с, reduced-motion=статика, «Скрыть на день» per-slot, owner/admin/staff не видят.
- [HOTFIX-FINAL-2] Бандл: route-level lazy для ВСЕХ экранов кроме лендинга (App.jsx); manualChunks: vendor/icons/charts/motion/ai/omega — recharts и framer-motion не должны попадать в initial. Цель: initial <1МБ (сейчас ~888КБ; index 556КБ, было 2.3МБ).
- [HOTFIX-FINAL-2] CORS: allowlist в `backend/config/cors.js` (env `CORS_ORIGINS`/`FRONTEND_URL` + дефолты), wildcard запрещён. Проверка curl: allowed origin → 204 + ACAO конкретного origin; чужой origin → 403 без ACAO.
- [HOTFIX-FINAL-2] AuthContext `/auth/me` — обязателен таймаут (AbortController 10с): без него TG WebView/спящий Render = вечный спиннер мини-аппа. Мини-апп: watchdog 12с → error-state с retry.
- [REAL-DATA] Owner-дашборд без моков: `backend/controllers/ownerController.js` отдаёт только реальные агрегаты (User/PlanConfig/Payment/`calcMRR`/`getFunnel`/expenseTracker), mock-фолбэки удалены (backend/data/ownerFallbackData.js удалён); `useOwnerData.js` стартует пустым, API авторитетен (пустой массив = пусто, НЕ подмена моком), localStorage-кэш для метрик запрещён (NO_CACHE_KEYS). График без данных не рисуется — empty-state. `formatCurrency` (owner/utils/helpers) по умолчанию ₽ ru-RU.
- [REAL-DATA] Реестр цен действий (✦): плоские поля OwnerSettings (chatMessage/linkAnalysis/visionAnalysis/tts/nicheCompetitors + videoAnalysis/coverGeneration/scriptGeneration), `getActionPrices/setActionPrices` кэш ≤60с. Owner GET/POST `/api/owner/action-prices` (ВЫШЕ generic `/:entity`), POST с маржинальным полом 70% → below_cost 400 «минимум N✦ при текущей себестоимости» (actionPricingService: себестоимость фактом из AiUsageLog.action 30д, нет логов → оценка 0.24₽/вызов; цена ✦ — входной пакет CreditPack). Клиент: GET `/api/omega/action-prices` + поле `actionPrices` в `/api/upload/limits`. Аналитика расхода 7/30д: `/api/owner/action-analytics` (ActionSpendLog пишется из consumeGeneration по `action`). consumeGeneration: `cost:0` = бесплатное действие (без списания), refundGeneration — фактическая цена.
- [REAL-DATA] consumeGeneration/refund НЕ трогаем без action-тега: AiUsageLog.action заполняется через `chatWithAI(..., { action })` → tryProviders → logAiUsage.
- [REAL-DATA] seed.js (мок-наполнение серверов/платежей) заблокирован без `ALLOW_MOCK_SEED=1`; старые мок-ряды в локальной БД вычищены. Композер чата на 360px: `flex-wrap` + `max-sm:min-w-[140px]` у omega-input — иначе ряд кнопок сжимал инпут до w=0 (сломан гейт З5).
- [REAL-DATA] Локальный e2e: backend поднимать с `OWNER_IP=127.0.0.1` (как CI — иначе матрица ролей выжигает rate-limit и ловит 429 на /auth/me), vite preview — `--host 127.0.0.1` (на Windows vite 6 встаёт на [::1], curl/некоторые клиенты не достукиваются).

- [COVERS-SUPREME] Пайплайн обложек уровня топов: `coverGenerator.js` оркестрирует: З1 niche-пресет из РЕАЛЬНЫХ топ-обложек ниши (`services/nicheStyleService.js` — youtubeDataService → thumbnail-паттерны по пикселям: доминантный насыщенный цвет/яркость/skin-доля/текст-энергия + эмоция из реальных заголовков; кэш TtlLruCache 30 мин; нет данных → UNIVERSAL_PRESET, обложка всегда собирается). З2 выбор момента по эмоции (`services/frameIntelligence.js`: skin-кластеры/saliency-bbox/энергия краёв всегда; vision Replicate LLaVA эмоция×1.4 поверх при ключе — эмоция побеждает контраст). З3 стикер-объект 30–50% высоты (`services/coverFx.js#buildSticker`: PRO cutout remove.bg→Replicate rembg, без ключа — органичная перо-эллипс маска (эллипс 84% bbox — иначе скруглённый прямоугольник с тёмным кольцом) + обводка ~4.5% высоты (blur+threshold раздутая альфа) + свечение). З4 цветокор (modulate+linear) + виньетка + радиальное свечение акцентом ниши + blur-fill (blur 40, brightness 0.5) при чужом aspect (log-ratio ≥0.16); PRO img2img — `stylizeBackgroundPro` (Replicate SDXL, только лучший кадр, только при vision-сцене; текста в промпте НЕТ никогда — `buildBackgroundPrompt`). З5 типографика `services/coverTypography.js`: шрифт **Russo One** (`backend/assets/fonts/RussoOne-Regular.ttf`, 39КБ, OFL; Anton/Bebas Neue кириллицы НЕ имеют — факт по charset) → SVG-пути через fontkit (fontconfig не нужен), UPPERCASE ≤3 строк, авто-фит максимизации fontSize (блок 25–40% кадра, ширина ≤90%), градиент + paint-order stroke + feGaussianBlur-свечение + 3D-экструзия, акцентное слово (число/hook-маркер) цветом пресета. З6 AI-скор `services/coverScore.js` (subject/emotion/textContrast/colorPop/readability 320px) → сортировка вариантов (лучший первый, `best:true`), выбор клиента → `POST /api/omega/cover-choice` → `models/CoverChoiceLog.js` → `getChoiceBias` (≥8 выборок) подстраивает порядок схем (schemeOrder) + write-through в procedural-слой памяти. З7 `GET /api/omega/cover-export?url&platform` — blur-fill переформат своего файла (чужой → 403), кнопки YouTube/Shorts/VK/TG под сеткой. З8 ключи: envMap `removebg` + карточка в ApiKeysTab (группа ai_image), PRO без ключа → базовый режим (не пусто/не ошибка/✦ за PRO не списываются). Гейты: qaOmegaVideo2 §5f (стикер+обводка пикселями, текст-блок ≥25%, blur-fill маркер двух цветов, strip текста из AI-промпта, choice-лог, экспорт ×4 + 403) + covers-frames-shots (бейдж скора, пресет-чип, cover-export ×4, stdev blur-полосы). Варианты НЕ переупорядочиваются по моменту — сортировка по AI-скору (гейт 5b: шумный кадр в выборке, скоры desc).
 

- [REAL-DATA-2] Legacy admin-refunds (`/api/admin/refunds*` + MonetizationTab) — ЖИВОЙ контур (решение владельца): переведён на реальный `createRefund` ЮKassa по паттерну adminRefundHandler (ключи ApiKeys, без env-фолбэков; без ключей/paymentId — отказ 400, mock-completed выпилен; сбой API → 502 + TG-алерт с HTML-экранированием). Заявки — Mongo `Refund` (in-memory запрещён). Негативы в qaSecurityFlow (B: client, C: staff → 403). Мёртвый `backend/config/payments.js` удалён; env-фолбэк ЮKassa в `utils/integrationStatus.js` выпилен.
- [REAL-DATA-2] Owner-кабинет без local-only CRUD: agents/campaigns/promos/news — реальный generic CRUD `/owner/<entity>` (AIAgent.id теперь default ObjectId — иначе POST падал валидацией, спавн из OMEGA Core был сломан молча). Нет серверного эндпоинта → честный error-тост (staff edit/delete — удаление данных = approve-зона; cabinets status/impersonate → view-as в шапке; integrations toggle). Недостижимые модалки (addTask/addApiKey/createAgent/sendEmail/omegaApproval — без единого открывателя) удалены вместе с хендлерами и state (tasks/apiKeys/approvalRequests). Хардкод-логи AgentsTab → empty-state.
- [REAL-DATA-2] `scripts/real-data-audit.mjs` — полный обход: 78 owner-табов + 9 creator-экранов + 2 advertiser × dark/light × 1280/360: мок-паттерны, сырые ключи, pageerror, горизонтальный скролл + часть B (реестр цен). Не входит в qa-launch — запускать отдельно при поднятых :18080/:4173.
- [KEYS-UNIVERSAL] Универсальный слот «Свой провайдер» (любой OpenAI-совместимый сервис — Kling/Veo/Kimi — владельцем за 2 мин без деплоя): модель `models/CustomProvider.js` (отдельная — у ApiKey жёсткий enum, его не расширять), сервис `services/customProviderService.js` (живая валидация GET {baseUrl}/models ДО сохранения — невалидный 400, молча не сохраняем; вызов OpenAI-совместимый chat/completions; TTL-кэш ротации 30с), роуты `routes/customProviders.js` `/api/custom-providers` owner-only (ключ в ответах только masked). Ротация чата: слоты (isActive+isValid+functions:'chat') идут в `tryProviders` ПОСЛЕ встроенной PROVIDER_CHAIN, до Local Brain. Мёртвый ключ слота: 401/402/403 → isActive=false + status=invalid в БД + TG-алерт (escapeHtml обязателен), фолбэк на следующих провайдеров. baseUrl только https (http://localhost|127.0.0.1 — тестовый шов для qa-stub). З4 «🔄 Проверить» на КАЖДОЙ карточке ApiKeysTab: POST `/api/api-keys/test-saved` (живой re-test сохранённого ключа, статус в БД). remove.bg — живая валидация GET /v1.0/account (не default-ветка). З6 playlist-анализ YouTube: `extractPlaylistId` + `fetchPlaylistInfo` в youtubeDataService (playlists.list + playlistItems.list + videos.list statistics, топ-5 по просмотрам, кэш 1ч); в omegaController плейлист-URL БЕЗ v= → отдельная ветка с честным «это плейлист из N видео», недоступно → честный отказ БЕЗ списания (паттерн YT-DATA-FIX). Гейты: `backend/scripts/qaCustomProviders.js` (входит в qa-launch, 25 проверок: guard 401/403, мёртвый endpoint не сохраняется, ротация, 401→auto-disable фактом, toggle, playlist) + `scripts/keys-universal-shots.mjs` (скрины UI, НЕ входит в qa-launch — при поднятых :18080/:4173). Ловушка qa: protect в NODE_ENV=development без Authorization → auto-owner (dev-bypass) — anon-негативы слать с битым токеном (`broken.token.here`).

- [STAFF-MGMT] Owner управляет staff-аккаунтами: PATCH/DELETE `/api/owner/staff/:id` owner-only (объявлены ВЫШЕ generic `/:entity/:id` в routes/owner.js), логика в `services/userManager.js#updateManagedUser/deleteManagedUser` — гарды на бэке: нельзя трогать owner-аккаунт, удалить/понизить себя; AuditLog `staff_updated`/`staff_deleted` с diff до/после; удаление = soft-delete с анонимизацией email. UI — TeamTab: редактирование модалкой, удаление только через confirm-диалог с именем и email аккаунта (`data-testid="staff-delete-confirm"`). Негативы в qaSecurityFlow (admin/client/staff → 403). Гейт — `scripts/staff-mgmt-shots.mjs` (не входит в qa-launch — запускать отдельно при поднятых :18080/:4173). `.bak`/`.backup` архивы в `frontend/src` запрещены (31 шт удалены — не роутятся, раздувают репо).
- [MEMORY-FIX] Потолок Render Free 512MB. In-memory кэши — ТОЛЬКО с TTL + лимитом записей: хелпер `backend/utils/ttlLruCache.js` (LRU + sweep); безразмерные Map/массивы на модульном уровне запрещены. Лог памяти: `backend/utils/memoryLog.js` (`[MEM] tag rss=… heap=…`, интервал 5 мин в healthMonitor + точечно vision/backup/upload). HealthMonitor: RAM WARN 420MB (console), FAIL 490MB, RAM-алерт не чаще 1/30мин и повтор только при росте RSS (не-RAM сбои — сразу). Бэкап zip — стриминг (`JSZip.generateNodeStream({streamFiles:true})`, gz не читаются в RAM). Upload `/api/upload/media` — multer diskStorage (видео до 250МБ стримится на диск, НЕ в heap; `/image` остался memoryStorage 10МБ для sharp). Sweep-интервалы ботов/кэшей — с `.unref?.()`.
- [SMART-TTL] Событийная очистка исходников (результаты — обложки/разбор/сценарии — вечны, не трогать): З1 «результат принят» (обложка скачана/применена к посту, драфт создан) → клиент зовёт `POST /api/upload/used {url, reason}` → `videoStorage.deleteMediaNow` удаляет исходник немедленно (только свой `/uploads/<userId>/`, cover-*.jpg отказ — инвентарь вечен) + TG-алерт владельцу (HTML-экранирование). З2 «клиент покинул задачу» — heartbeat `POST /api/upload/heartbeat {url}` раз в 30с из OmegaChat (только при TTL>0, цель = последний `action.videoUrl` в ленте; URL переживает F5 — strip касается только кадров); молчание > `OwnerSettings.videoIdleMinutes` (дефолт 30, 1–1440, hot-reload ≤60с, карточка в Подписках рядом с TTL, клиент читает в `/api/upload/limits`) → idle-свип в `cron/mediaCleanup.js` (0c, только kind video + lastSeenAt ≠ null). З3 TTL-потолок (deleteAt, свип 0) срабатывает при ЛЮБОМ heartbeat. З4 мягкие уведомления в чат: `chat.sourceTtlNotice` (после разбора при TTL>0, ДО удаления) и `chat.sourceClearedNotice` (после used) — i18n ×4 файла. З5 все удаления → TG-лог владельцу + storage-usage фактом с диска. Гейт: `backend/scripts/qaSmartTtl.js` (входит в qa-launch).
- [COVERS-FRAMES] Обложки: дефолт = РЕАЛЬНЫЙ кадр, не text-to-image. `generateCoverVariants({mode:'frame'|'ai'})`: mode frame — 3 лучших кадра по sharp-скору (контраст σ + энтропия + центр-буст, `pickBestFrames`) из dataURL-кадров, которые клиент УЖЕ извлёк для vision-разбора (`action.frames` в msg видео-разбора; в localStorage-историю кадры НЕ пишутся — strip в saveHistory useOmegaChat), либо официальный thumbnail YouTube (`fetchYouTubeThumbnail`: maxresdefault→sddefault→hqdefault по факту — 404/<6КБ/<320px = следующее качество; `extractYouTubeId` — только youtube/youtu.be домены). Оверлей: скрим-градиент + обводка (paint-order stroke) + плашка в композиции 0, 3 композиции (bottom-center/bottom-left/top-left). Pollinations НЕ удалён: кнопка «🎨 Перегенерировать стиль» (mode:'ai', source:'ai'), дефолт всегда кадр. Цена — прежняя `coverGenerationCostCredits` (hot-reload), реестр не дублируется. Вариант несёт `source`('frame'|'youtube'|'ai')/`frameIndex`. Кнопка обложек показывается и для YouTube-карточек (`msg.videoAnalysis.videoId`). Гейт — qaOmegaVideo2 §5b (frame-режим, скор, extractYouTubeId, ai-режим).
- [COVERS-FRAMES ДОР] Разрешение фона: клиент извлекает ДВА набора кадров одних таймкодов (`extractVideoFrames`): 640px (vision+скор, `action.frames`) и полноразмерные до 1920 по ширине, без апскейла выше исходника (jpeg 0.8, `action.fullFrames`, strip в saveHistory вместе с frames). Сервер (`/api/omega/cover-variants` принимает `fullFrames` ≤6 × ≤4.5МБ base64): скор по 640px, рендер из `fullFrames[frameIndex]` (`composeCover noUpscale` — оверлей строится по ФАКТИЧЕСКИМ размерам после resize, иначе composite падает; вариант отдаёт реальные width/height + `bgFullRes`). Нет fullFrames → легаси-рендер из 640px.
- [COVERS-FRAMES ДОР] Битая обложка — 3 корня, все закрыты: (1) `helmet()` ставил `Cross-Origin-Resource-Policy: same-origin` → на проде `<img>` с aiviral-backend.onrender.com резался браузером: для `/uploads` CORP=cross-origin (server.js); (2) mediaCleanup удалял обложки (сирота-свип analyzedAt null >24ч + 7-дневный/лимитный fs-свип) — теперь MediaFile kind `cover` + `analyzedAt` при записи, `cover-*.jpg` пропускаются во ВСЕХ свипах (`isCoverFile` в cron/mediaCleanup.js), TTL исходного видео на обложки НЕ распространяется (инвентарь вечен); (3) сетка была in-memory (`coversByMsg`) → после F5 пропадала: результат — персистентное сообщение `action.type:'covers'` (variants = только URL) в истории чата, регенерация обновляет его на месте (`updateMessage` в useOmegaChat, доезжает в OmegaChat через спред `{...chat}`), выбор варианта — локальный state по id сообщения; кадры для регенерации — по `sourceMsgId` в живой ленте (после F5 stripped → честный AI-фолбэк). qaOmegaVideo2 §5c (зелёный маркер full-res 1920×1080, noUpscale 960×540 не растёт, крон не трогает cover-*.jpg) + covers-frames-shots (ширина фона ≥1280, маркер, картинка 200 и naturalWidth>0 после F5).
- [COVERS-FRAMES ДОР-2] Различимость 3 вариантов при ОДНОМ базовом кадре (YouTube-thumbnail, повтор кадра при нехватке): `variantCrop` в composeCover — вариант 0 кадр целиком, 1 зум к центру 0.7 с уклоном вверх (лицо крупнее), 2 смещение вправо-вниз 0.78 (зона под текст top-left свободна); `textOverlaySvg` — разные схемы: v0 белый+плашка+скрим 0.72, v1 акцентный #ffd60a+скрим 0.84, v2 белый+скрим 0.56 сверху. Гейты: qaOmegaVideo2 §5d (один кадр → попарный пиксельный diff 64×36 > 8) + covers-frames-shots (diff сетки > 6 по загруженным URL).
 
- [YT-FRAMES] По YouTube-ссылке обложки собираются из КАДРОВ самого видео: `backend/services/ytFrames.js` — yt-dlp ТОЛЬКО скачивает (`bestvideo[height<=720]` — видео-only, комбинированных форматов у YT больше нет; ≤15 мин probe, ≤150МБ, таймауты 30/180с, диск-гард ≥400МБ свободно, семафор max 2 параллельных), ffmpeg-static режет 9 кадров равномерно (5–95% длительности) в нативном разрешении потока; workdir в os.tmpdir() удаляется СРАЗУ в finally (диск Render). Кэш кадров TtlLruCache(8 видео, 30 мин) — регенерация не качает заново. Бинарь yt-dlp: env YTDLP_PATH → PATH → ленивая загрузка pinned-версии (2026.08.19 — у 2025.x probe падает «The page needs to be reloaded») в tmpdir/aiviral-bin. Любой сбой (anti-bot, приватное/возрастное/стрим, лимиты, нет бинаря) → null + `getLastYtFramesSkip()` reason + строка `[yt-frames] fallback <id>: <reason>` в лог (НЕ молча — инцидент 15.09 диагностировался вслепую) → прежний thumbnail-фолбэк, клиенту НЕ ошибка; coverGenerator логирует переход yt-frames→thumbnail→AI. Kill-switch `YT_FRAMES_DISABLE=1`. В coverGenerator YouTube-ветка: yt-frames → pickBestFrames × композиции, `noUpscale` только v0 (зум-кропы v1/v2 увеличивают область до размера платформы — иначе 720p-кадр × кроп 0.7 давал обложку 896×504), source='youtube-frames', frameIndex. Тестовый шов `ytFramesProvider` (CI без сети/yt-dlp). Гейты: qaOmegaVideo2 §5e (9 кадров нативных, файл удалён фактом, 3 разных frameIndex + diff > 8, фолбэк, disabled/bad_id → null) + covers-frames-shots (source=youtube-frames живьём, 3 разных момента, размер платформы, недоступное видео → фолбэк-цепочка без ошибки; таймауты proxy/grid подняты до 150–180с под скачивание ~60–90с).

- [YT-DATA-FIX] Инцидент 15.09 (4 YT-ссылки → «нет данных», yt-frames молча в AI-фолбэк): корни — keyGuard был подключён только для replicate (youtube-ошибки не отключали ключ и не алертили), `getYoutubeApiKey` воскрешал env после getProviderKey (запрещённый фолбэк HOTFIX-FINAL-2, выпилен — env-запас уже внутри getProviderKey), чат при недоступной статистике звал AI и списывал ✦. Теперь: постоянные ошибки Google (keyInvalid/accessNotConfigured/ipRefererBlocked/forbidden) → `disableProviderKey('youtube', …)` (isActive=false + TG-алерт + карточка invalid в кабинете); quotaExceeded/dailyLimitExceeded — ключ НЕ трогаем, троттленный TG-алерт ≤1/6ч; no_api_key → тот же троттленный алерт. Чат: YT-ссылка + статистика недоступна + нет явного действия (обложка/драфт/best time) → честный отказ «Не удалось получить данные YouTube: <причина>» БЕЗ вызова AI и БЕЗ списания (refund фактической цены, honest:true source:youtube-unavailable + карточка со statsError). Parse-регресс (watch/shorts/youtu.be + si-параметр) — qaOmegaVideo2 §5b/§8. Гейт скринов: `scripts/yt-data-shots.mjs` (не входит в qa-launch — при поднятых :18080/:4173). yt-dlp на Shorts с residential IP работает (probe+download 18с, 9 кадров); на проде (Render, datacenter IP) вероятен anti-bot — по новой строке лога reason видно фактом.

 


- [KNOWLEDGE-PACK] База вирусных знаний: `backend/knowledge/*.json` (hooks — 21 формула en+ru match-паттерны, titles — Y1–Y6/Y11 2026, packaging — пара-правила, seo — скелет/главы/теги, platforms — спеки площадок, algorithm — эвристики/тайминг/кратность-к-медиане, cis — СНГ-паттерны) — MIT-источники (Jakeschincariol/youtube-agent-skill, sergebulaev/youtube-skills), hot-reload ≤60с по mtime через `services/knowledgeService.js` (buildHooksBlock/buildTitlesBlock/buildSeoBlock/buildPackagingBlock/buildAlgorithmBlock/buildCisBlock/getPlatformSpec). Детерминированный скоринг без LLM — `services/viralScorer.js` (порт hookscore.py: 5 свойств, вердикт 60% среднего + 40% слабейшего; title.py-линт; lintPair дубля тайтл↔обложка; rankTitles/rankHooks — лучший первым). Вайринг: aiService.generateContent (все типы), youtubeAI generateTitles (варианты со скором)/generateShortsScript, omega.js generate-hook/script-from-idea/analyze-video-upload, bestTimeService (окна тайминга), niche-competitors (метод кратности). VK/Дзен исключены (решение владельца) — getPlatformSpec вернёт null. З2 линт пары: cover-variants принимает videoTitle, дубль значимых слов вырезается из текста обложки ДО генерации (фикс только если осталось ≥1 значимое слово, иначе duplicate-флаг честно остаётся), pairCheck в ответе. З3 meta-package: POST /api/omega/meta-package — ОДНА генерация → пакеты по языкам (OwnerSettings.packagingLanguages через getPackagingLanguages, EN ВСЕГДА первым — приоритет зарубеж; owner GET/POST /api/owner/knowledge-settings объявлены ВЫШЕ generic /:entity; клиент читает в /api/upload/limits) × площадкам из platforms.json; формулы пересобираются под язык (RU — с cis.json), НЕ дословный перевод; AI-фейл/не-JSON → честный success:false (ai_unavailable/ai_bad_format). З4 Self-Optimize: POST /api/omega/meta-choice → models/MetaChoiceLog.js (паттерн CoverChoiceLog: байас формул, agreeRate, ≥8 выборок, write-through в procedural-память). Гейт: backend/scripts/qaKnowledge.js (входит в qa-launch, 30 проверок). Цены: meta-package пока без ✦-списания — ждёт решения владельца (approve-зона).

- [DESIGN-PRO] `backend/knowledge/design.json` — пороги UX (8 законов, Refactoring UI, conv-1..6, WCAG AA, mobile 44px/16px, states, microFinish ≤200ms). Аудит-гейт `scripts/design-pro-audit.mjs` (НЕ в qa-launch — при поднятых :18080/:4173; тема app-экранов через PUT /users/me preferences.theme — профиль перекрывает localStorage; тур гасится ключом `omega_onboarding_tour_done`). Канон состояний: `components/common/EmptyState.jsx` (стандарт) + `components/common/ErrorState.jsx` (что случилось + retry, role=alert); `shared/EmptyState`/`shared/LoadingSkeleton` удалены как мёртвые дубли. Interaction-переходы ≤200ms везде (`.reveal` 0.7s — entrance, осознанно); иконочные кнопки ≥40px (iconButtonMinPx), текстовые ≥44px; inline-ссылки в тексте — исключение WCAG. Регистрация — 3 поля (без «Повторите пароль», qa-journey-1 обновлён). Light-тема: `.chat-pro-page` оверрайды в luxury.css стоят ДО `.chat-pro-embed` (source order), хардкод text-white в layout запрещён — только var(--text).

## Актуализация AGENTS.md (постоянное правило)

- AGENTS.md читается в начале каждой сессии. По ходу работы сам вносишь новые правила/факты и удаляешь устаревшее.
- Конфликт правил (AGENTS.md vs ТЗ батча) — строкой в отчёт + ask-owner.

## Стандарты

- Новые экраны — по `docs/UI_QUALITY_GATE.md`: Esc/focus-trap через `useModalA11y`, тултипы у обрезанного, стили только дизайн-системы, reduced-motion, 360/428/768/1280/1920 × RU/EN без скроллов/налезаний, empty-state inline.
- i18n: ключи во все 4 locale-файла (`frontend/src/locales/{ru,en}.json`, `frontend/public/locales/{ru,en}.json`), `i18n-parity` обязателен.
- Секреты — только env. Никаких ключей/токенов/паролей в коде и коммитах.
- Логика не меняется, если задача про представление — и наоборот: объём задачи не расширять самовольно.

## ПРАВИЛО ЭКОНОМИКИ

- Цены меняются только через кабинет владельца / `PlanConfig`. Кодом значения тарифов не править.
- Себестоимость 1 кредита ≈ 0.24₽ — учитывать в любых расчётах фич, расходующих генерации.
- Любая новая платная фича — с расчётом маржи в отчёте. Маржинальный пол: платные тарифы не ниже 70%.
- Перед изменением цен (даже обоснованных) — согласование владельца.

## CI

- `.github/workflows/ci.yml`: на PR и пуш в `fix/*`/`ci/*`/`main` — `node --check` backend, frontend build, прогон `scripts/qa-launch.mjs` (Mongo service + backend :18080 + preview :4173). Падение любого шага = PR красный.
- `node scripts/qa-launch.mjs` — «одна кнопка» перед запуском/сдачей: все qa-скрипты последовательно, сводка скрипт → ✅/❌ → время. Новые `scripts/qa-*.mjs` и `backend/scripts/qa*.js` подхватываются автоматически.
